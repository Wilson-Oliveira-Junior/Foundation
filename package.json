{
  "name": "foundation-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5174",
    "start-offline": "npx serve dist -s"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  ,
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}

