Content Packs directory

- Place packs under `pack-<name>-v<version>` folders.
- Each pack should contain `manifest.json` and `questions.json` (or templates/variables files).

Example structure:

```
Foundation/content-packs/
  pack-programming-v1/
    manifest.json
    questions.json
```
